<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/candidate_interview"
        android:label="@string/app_name"
        android:roundIcon="@drawable/candidate_interview"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        tools:targetApi="31">
        <activity
            android:name=".view.onboarding.FirstScreenActivity"
            android:exported="true">
            <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".view.onboarding.SecondScreenActivity"
            android:exported="true">
        </activity>
        <activity
            android:name=".view.onboarding.ThirdScreenActivity"
            android:exported="true">
        </activity>
        <activity android:name=".view.login.LoginScreenActivity"
            android:exported="true">
        </activity>
    </application>
</manifest>